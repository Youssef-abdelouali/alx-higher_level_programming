#!/usr/bin/node

// Array of strings containing the lines to print
const lines = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

// Loop through the array and print each line using console.log
for (const line of lines) {
  console.log(line);
}
