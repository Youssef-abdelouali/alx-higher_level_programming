-- This SQL command creates a table named first_table with columns id and name in the MySQL server, if it doesn't already exist.
CREATE TABLE IF NOT EXISTS `first_table` (`id` INT, `name` VARCHAR(256));
