-- This SQL command retrieves all records from the table named second_table.
-- The records are sorted in descending order based on the score attribute.
SELECT `score`, `name`
FROM `second_table`
ORDER BY `score` DESC;
